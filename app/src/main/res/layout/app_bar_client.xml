<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.thomas.mirakle.crm_mirakle.client">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay"
            app:title="Home" />

    </android.support.design.widget.AppBarLayout>

    <include
        android:id="@+id/home_for_client"
        layout="@layout/content_client"
        android:visibility="visible" />

    <include
        android:id="@+id/about_for_client"
        layout="@layout/about"
        android:visibility="invisible" />

    <include
        android:id="@+id/rate_app_for_client"
        layout="@layout/rate_app"
        android:visibility="invisible" />

    <include
        android:id="@+id/lead_for_client"
        layout="@layout/lead"
        android:visibility="invisible" />

    <include
        android:id="@+id/history__for_client"
        layout="@layout/history"
        android:visibility="invisible" />

    <include
        android:id="@+id/feedback"
        layout="@layout/feedback"
        android:visibility="invisible" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        app:srcCompat="@android:drawable/ic_dialog_email" />

</android.support.design.widget.CoordinatorLayout>
